package Base.src;

import java.util.ArrayList;
import java.util.List;

/**
 * 基本的运算，按照优先级排序如下(同行同级)：
 * 
 * () ! ~ ++ -- * / % + - << >> >>> & | += -= *= /=
 * 
 */
public class Arithmetic {
    public static void main(final String[] args) {
    }

}